- **BC-33 复用母版当正面用字节拷贝，上层资产中心串号**：G2 正面复用母版时若直接复制文件（字节完全相同），契约 v3 的 `--assign-ids` 按内容指纹去重，会把母版与正面判成同一 asset_id，触发 E8（跨身份串号）。修法：正面即使视觉复用母版，也要经 `kit_standard_cell.py` 重新落格产出独立文件（重新编码即不同指纹）；若已入库才发现，给正面行手工分配独立 asset_id（按路径稳定，后续工具会保留）。
