# 贡献指南

欢迎提 issue 与 PR。这个项目的一半价值是"方法论"，另一半是"确定性脚本"，两者的贡献标准不同，请先判断你的改动属于哪一类。

## 我们收什么

1. **新的 bad case（最受欢迎）**：你在真实生产中踩到的崩图、台账、拼板问题。
2. **提示词改写规则的证据与修正**：必须有受控复现依据，不接受"我试了一张成了"。
3. **脚本缺陷修复与健壮性改进**：需要带 unittest。
4. **新板型 / 新 profile**：通用、可复用的才收；只服务某个私有角色的不收。
5. **文档修正、翻译（英文 PR 欢迎）**。

## 不收什么

- 绑定具体私有角色、私有品牌、内部路径的内容（仓内零私有信息是硬红线）。
- 引入新的必需重依赖（OpenCV、rembg、numpy 之外的模型权重等）的改动。Pillow 是唯一必需图像依赖；增强能力请走可选路径。
- 需要联网、需要付费 API key 才能跑的脚本逻辑。
- 把审美判断写死进脚本的改动（脚本只做确定性工序）。
- 整板提示词"一次出三视图/表情墙"类方法论回退——这是本项目明确否定的路线。
- 未授权素材、可识别在世艺术家/品牌的模仿模板。

## 各类改动的具体要求

### A. 新增 bad case（references/bad-cases.md）

每条四段：**现象（含触发条件）／根因／修法（提示词层或脚本层）／适用范围**。
- 现象必须可复现：写清模型/运行时、画幅、参考图喂法、失败率（"10 张里 6 张"而不是"经常"）。
- 只发生一次、无法复现的，先记在你自己的项目里，攒到复现证据再提。
- 编号顺延（当前到 BC-33）。

### B. 改提示词方法论（references/prompt-*.md）

- 结论需要对照证据：至少同一指令的多图结果，最好有"改前/改后"两组对比。
- 区分三类结论：信息缺失（改提示词即可）、模型世界知识/人体几何（必须提示词＋参考图）、模型能力边界（怎么写都不行，应改流程）。
- 规则要写成可执行的祈使句，不写玄学形容词。

### C. 改脚本（scripts/）

- 标准库＋Pillow 能实现就不加依赖；macOS 专属能力必须有非 macOS 的文档化替代。
- 新行为、新守卫必须在 `tests/` 加 unittest；修 bug 先写一个能在旧代码上失败的测试。
- 本地验证：
  ```bash
  python3 -m unittest discover -s tests -v          # 全绿
  python3 -m py_compile scripts/charkit/*.py scripts/bin/*.py
  # 端到端冒烟：临时目录 init 新库 → scaffold → check 必须 0 ERROR
  ```
- 脚本不得打印/写入任何私有角色名或绝对路径；示例一律用 `CHAR-01-demo`、相对路径。
- 板型输出是定尺 PNG：改几何后要说明尺寸是否变化，并更新对应 references 与测试断言。

### D. 新板型

- 先开 issue 说明：它服务哪个门、为什么不能用现有板型组合替代、输入清单配置长什么样。
- 同时提供：`charkit.boards` 构建函数、`kit_build_board.py` 注册、模板 `boards` 段、门径文档、测试、干净版/验收版两种输出。

### E. 新 profile

- profile 是"画风＋门集合＋插槽＋画幅"的打包；默认盲盒 3D profile 始终只是 example。
- 新 profile 不得包含私有角色身份；身份属于角色包的规格卡，不属于 profile。

## 提交前自查

- [ ] `tests/` 全绿、`py_compile` 全过、全新库冒烟 0 ERROR
- [ ] 没有提交 `subjectmask` 二进制、`datasets/`、`__pycache__`、`.DS_Store`、raw 大图
- [ ] 全仓 grep 无私有角色名、内部路径、内部品牌
- [ ] 用户可见文字是中文（英文翻译同步或另提均可）
- [ ] CHANGELOG.md 增加一条（用户视角的变化，不是 commit 流水账）

## 行为准则

就事论事，对事不对人；讨论围绕可复现证据与用户价值。
